<UserControl x:Name="UserControl" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:cal="http://www.caliburnproject.org"
    xmlns:next="clr-namespace:Unbroken.LaunchBox.Windows.Desktop;assembly=LaunchBox"
    xmlns:system="clr-namespace:System;assembly=mscorlib"
    xmlns:controls="clr-namespace:Unbroken.LaunchBox.Windows.Controls;assembly=Unbroken.LaunchBox.Windows"
    xmlns:win="clr-namespace:Unbroken.LaunchBox.Windows;assembly=Unbroken.LaunchBox.Windows"
    xmlns:converter="clr-namespace:Unbroken.LaunchBox.Windows.Converters;assembly=Unbroken.LaunchBox.Windows"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="300"
    Focusable="False"
    FontFamily="{Binding FontFamily}"
    FontSize="{Binding FontSize}"
    FontWeight="{Binding FontWeight}"
    FontStyle="{Binding FontStyle}">
    <UserControl.Resources>
        <win:BindingProxy x:Key="IconVisibility"  Data="{Binding IconVisibility}"/>
        <converter:ToUpperConverter x:Key="UpperCase" />
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />
        <Style x:Key="ExpandCollapseToggleStyle" TargetType="ToggleButton">
            <Setter Property="Focusable" Value="False" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ToggleButton">
                        <Grid Width="20" Height="13" Background="Transparent">
                            <Path x:Name="ExpandPath" HorizontalAlignment="Left" VerticalAlignment="Center" Margin="6,2,1,1"
                                Fill="{Binding ElementName=Items, Path=Foreground}" Data="M 4 0 L 10 5 L 4 10 Z" />
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter Property="Data" TargetName="ExpandPath" Value="M 2 3 L 12 3 L 7 9 Z" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="{x:Type ToolTip}" TargetType="ToolTip">
            <Setter Property="OverridesDefaultStyle" Value="True"/>
            <Setter Property="HasDropShadow" Value="False"/>
            <Setter Property="FontSize" Value="14" />
            <Setter Property="FontFamily" Value="LAUNCHBOX_THEME_FOLDER/Fonts/Poppins-Regular.ttf#Poppins" />
            <Setter Property="Foreground" Value="#E8E8E8"/>
            <Setter Property="Background" Value="#00000000"/>
            <Setter Property="BorderBrush" Value="#00000000"/>
            <Setter Property="AllowDrop" Value="True"/>
            <Setter Property="Placement" Value="Center"/>
            <Setter Property="StaysOpen" Value="True"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ToolTip">
                        <Border Background="#0E1014" CornerRadius="8" BorderBrush="#33363F" BorderThickness="1">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" Opacity="0.87" Margin="10 5 10 5" />
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="TreeViewItemFocusVisual">
            <Setter Property="Control.Template">
                <Setter.Value>
                    <ControlTemplate>
                        <Border>
                            <Rectangle Margin="0,0,0,0" StrokeThickness="1" Stroke="{Binding ForegroundBrush}" />
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style TargetType="{x:Type TreeViewItem}">
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="HorizontalContentAlignment" Value="{Binding Path=HorizontalContentAlignment, RelativeSource={RelativeSource AncestorType={x:Type ItemsControl}}}" />
            <Setter Property="VerticalContentAlignment" Value="{Binding Path=VerticalContentAlignment, RelativeSource={RelativeSource AncestorType={x:Type ItemsControl}}}" />
            <Setter Property="Padding" Value="1,0,0,0" />
            <Setter Property="Foreground" Value="{Binding ElementName=Items, Path=Foreground}" />
            <Setter Property="FocusVisualStyle" Value="{StaticResource TreeViewItemFocusVisual}" />
            <Setter Property="IsSelected" Value="{Binding Selected, Mode=TwoWay}" />
            <Setter Property="IsExpanded" Value="{Binding Expanded, Mode=TwoWay}" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type TreeViewItem}">
                        <ControlTemplate.Resources>
                            <next:LeftMarginMultiplierConverter Length="19" x:Key="LengthConverter" />
                        </ControlTemplate.Resources>
                        <StackPanel>
                            <Grid>
                                <Border Name="BColor" Width="5" CornerRadius="2.5" Background="Transparent" HorizontalAlignment="Left" RenderTransformOrigin="0.5 0.5" />
                                <Border Name="Bd" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="0" Padding="{TemplateBinding Padding}">
                                    <Grid Margin="{Binding Converter={StaticResource LengthConverter}, RelativeSource={RelativeSource TemplatedParent}}">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="{Binding ElementName=TreeViewToggleButtonWidthBorder, Path=Width}" />
                                            <ColumnDefinition />
                                        </Grid.ColumnDefinitions>
                                        <ToggleButton x:Name="Expander" Style="{StaticResource ExpandCollapseToggleStyle}"
                                        IsChecked="{Binding Path=IsExpanded, RelativeSource={RelativeSource TemplatedParent}}" ClickMode="Press" />
                                        <ContentPresenter x:Name="PART_Header" Grid.Column="1" ContentSource="Header" Margin="-3,0,0,0"
                                        HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" />
                                    </Grid>
                                </Border>
                            </Grid>
                            <ItemsPresenter x:Name="ItemsHost" />
                        </StackPanel>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsExpanded" Value="false">
                                <Setter TargetName="ItemsHost" Property="Visibility" Value="Collapsed" />
                            </Trigger>
                            <Trigger Property="HasItems" Value="false">
                                <Setter TargetName="Expander" Property="Visibility" Value="Hidden" />
                            </Trigger>
                            <MultiTrigger>
                                <MultiTrigger.Conditions>
                                    <Condition Property="HasHeader" Value="false" />
                                    <Condition Property="Width" Value="Auto" />
                                </MultiTrigger.Conditions>
                                <Setter TargetName="PART_Header" Property="MinWidth" Value="75" />
                            </MultiTrigger>
                            <MultiTrigger>
                                <MultiTrigger.Conditions>
                                    <Condition Property="HasHeader" Value="false" />
                                    <Condition Property="Height" Value="Auto" />
                                </MultiTrigger.Conditions>
                                <Setter TargetName="PART_Header" Property="MinHeight" Value="19" />
                            </MultiTrigger>
                            <Trigger Property="IsSelected" Value="true">
                                <Setter TargetName="BColor" Property="Background" Value="{Binding ElementName=SelectionBorder, Path=Background}" />
                                <Setter TargetName="Bd" Property="BorderThickness" Value="0" />
                                <Setter TargetName="Bd" Property="Background" Value="#21FFFFFF" />
                                <Setter TargetName="Bd" Property="CornerRadius" Value="0 15 15 0" />
                            </Trigger>
                            <MultiTrigger>
                                <MultiTrigger.Conditions>
                                    <Condition Property="HasHeader" Value="True" />
                                    <Condition Property="IsMouseOver" Value="true" SourceName="Bd" />
                                    <Condition Property="IsExpanded" Value="True"/>
                                </MultiTrigger.Conditions>
                                <Setter TargetName="Bd" Property="Background" Value="#11FFFFFF" />
                                <Setter TargetName="Bd" Property="BorderThickness" Value="0" />
                                <Setter TargetName="Bd" Property="CornerRadius" Value="15" />
                            </MultiTrigger>
                            <MultiTrigger>
                                <MultiTrigger.Conditions>
                                    <Condition Property="IsMouseOver" Value="true" />
                                    <Condition Property="IsExpanded" Value="false" />
                                </MultiTrigger.Conditions>
                                <Setter TargetName="Bd" Property="Background" Value="#11FFFFFF" />
                                <Setter TargetName="Bd" Property="BorderThickness" Value="0" />
                                <Setter TargetName="Bd" Property="CornerRadius" Value="15" />
                            </MultiTrigger>
                            <MultiTrigger>
                                <MultiTrigger.Conditions>
                                    <Condition Property="IsMouseOver" Value="true" />
                                    <Condition Property="IsSelected" Value="true" />
                                </MultiTrigger.Conditions>
                                <Setter TargetName="BColor" Property="Background" Value="{Binding ElementName=HoverSelectionBorder, Path=Background}" />
                                <Setter TargetName="Bd" Property="BorderThickness" Value="0" />
                                <Setter TargetName="Bd" Property="CornerRadius" Value="0 15 15 0"/>
                                <Setter TargetName="Bd" Property="Background" Value="#21FFFFFF" />
                            </MultiTrigger>
                            <MultiTrigger>
                                <MultiTrigger.Conditions>
                                    <Condition Property="IsSelected" Value="true" />
                                    <Condition Property="IsSelectionActive" Value="false" />
                                </MultiTrigger.Conditions>
                            </MultiTrigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="FocusVisualStyle">
            <Setter Property="Control.Template">
                <Setter.Value>
                    <ControlTemplate>
                        <Rectangle StrokeThickness="1" Stroke="{Binding ForegroundBrush}" SnapsToDevicePixels="true" />
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>
    <Grid x:Name="Grid" Background="#15171C">
        <Grid.RowDefinitions>
            <RowDefinition Height="20" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="20" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="100" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Border Grid.Row="0" x:Name="SelectionBorder" Background="{Binding SelectionBackgroundBrush}" Visibility="Collapsed" />
        <Border Grid.Row="0" x:Name="HoverSelectionBorder" Background="{Binding HoverSelectionBackgroundBrush}" Visibility="Collapsed" />
        <Border Grid.Row="0" x:Name="HoverBorder" Background="#22FFFFFF" Visibility="Collapsed" />
        <Border Grid.Row="0" x:Name="TreeViewToggleButtonWidthBorder" Width="{Binding TreeViewToggleButtonWidth}" Visibility="Collapsed" />
        <Border Grid.Row="0" x:Name="ComboHoverBorder" Background="#22FFFFFF" Visibility="Collapsed" />
        <Grid Grid.Row="1" Grid.Column="0" Margin="20, 0, 20, 0" Background="Transparent">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <Border Grid.Column="0" Grid.ColumnSpan="2" CornerRadius="4" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
                <Border.Style>
                    <Style TargetType="Border">
                        <Setter Property="Background" Value="#22FFFFFF" />
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding ElementName=Search, Path=IsFocused}" Value="True">
                                <Setter Property="Background" Value="#33FFFFFF" />
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </Border.Style>
            </Border>
            <TextBlock Grid.Column="0" Margin="5,2" Text="{Binding SearchLabel}" Foreground="{Binding FadedSearchForegroundBrush}" Padding="9, 6, 0, 0"
                       Visibility="{Binding ElementName=Search, Path=Text.IsEmpty, Converter={StaticResource BooleanToVisibilityConverter}}" />
            <TextBox Grid.Column="0" x:Name="Search" Background="Transparent" Foreground="{Binding SearchForegroundBrush}" CaretBrush="{Binding SearchForegroundBrush}"
                     BorderThickness="0" Text="{Binding Path=SearchText, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Padding="12, 8, 1, 8" FocusVisualStyle="{StaticResource FocusVisualStyle}" ToolTipService.InitialShowDelay="0" ToolTipService.BetweenShowDelay="0" />
            <Button Grid.Column="1" Name="SearchClear" ToolTip="Clear" Width="33" Background="Transparent" BorderThickness="0"
                    HorizontalAlignment="Right" VerticalAlignment="Stretch" Visibility="{Binding SearchClearVisibility}" FocusVisualStyle="{StaticResource FocusVisualStyle}">
                <Grid>
                    <Polygon Points="12,0 14,0 17,3 20,0 22,0 18,4 22,8 20,8 17,5 14,8 12,8 16,4"
                        StrokeThickness="0" Fill="White" />
                </Grid>
                <Button.Style>
                    <Style TargetType="Button">
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="{x:Type Button}">
                                    <Border Name="Border" BorderThickness="0" Background="{TemplateBinding Background}" CornerRadius="5">
                                        <ContentPresenter HorizontalAlignment="Left" VerticalAlignment="Center" />
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger SourceName="Border" Property="IsMouseOver" Value="True">
                                            <Setter TargetName="Border" Property="Background" Value="#33FFFFFF" />
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </Button.Style>
            </Button>
            <Button x:Name="OpenFiltersMenu" Grid.Column="2" Margin="6,0,0,0" Padding="6" Background="#22FFFFFF" Width="33" BorderThickness="0" FocusVisualStyle="{StaticResource FocusVisualStyle}">
                <Button.ToolTip>
                    <ToolTip Placement="Mouse">
                        <TextBlock Text="{Binding FilterLabel}" />
                    </ToolTip>
                </Button.ToolTip>
                <Button.Style>
                    <Style TargetType="Button">
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="{x:Type Button}">
                                    <Border Name="Border" BorderThickness="0" Background="{TemplateBinding Background}" CornerRadius="5">
                                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger SourceName="Border" Property="IsMouseOver" Value="True">
                                            <Setter TargetName="Border" Property="Background" Value="#33FFFFFF" />
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </Button.Style>
                <Path Height="14" Width="14" Stretch="Fill" HorizontalAlignment="Center" VerticalAlignment="Center" Data="M52.5,49.7839661 C52.5,49.2326468 52.7729285,48.4348062 53.1182899,47.9907701 L63.3817101,34.7949442 C63.7231822,34.3559086 63.5601869,34 63.0034652,34 L36.9965348,34 C36.4461638,34 36.2729285,34.3509081 36.6182899,34.7949442 L46.8817101,47.9907701 C47.2231822,48.4298056 47.5,49.2285373 47.5,49.7856334 L47.5,65.0000809 C47.5,65.552321 47.909171,65.8363316 48.4287437,65.6285025 L51.5712563,64.3714975 C52.0841873,64.1663251 52.5,63.5610739 52.5,63.0017482 L52.5,49.7839661 Z M55.5,63.0017482 C55.5,64.7880882 54.3370973,66.49626 52.6854283,67.1569276 L49.5429157,68.4139326 C47.0501167,69.4110522 44.5,67.6719212 44.5,65.0000809 L44.5,49.7856334 C44.503851,49.8205295 34.2502333,36.6367661 34.2502333,36.6367661 C32.3804092,34.2327065 33.9362585,31 36.9965348,31 L63.0034652,31 C66.0819275,31 67.608786,34.2465984 65.7497667,36.6367661 L55.4863466,49.8325919 C55.5006278,49.8060358 55.5,63.0017482 55.5,63.0017482 Z">
                    <Path.Style>
                        <Style TargetType="Path">
                            <Setter Property="Fill" Value="#90FFFFFF"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding FiltersApplied}" Value="True">
                                    <Setter Property="Fill" Value="#39FF14" />
                                </DataTrigger>
                                <DataTrigger Binding="{Binding ElementName=OpenFiltersMenu, Path=IsMouseOver}" Value="True">
                                    <Setter Property="Fill" Value="#E0FFFFFF"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Path.Style>
                </Path>
            </Button>
        </Grid>
        <Border Grid.Row="3" Grid.RowSpan="2" Background="#0E1014" BorderBrush="#33363F" BorderThickness="1" CornerRadius="4" Margin="20,-20,59,0" Panel.ZIndex="15">
            <Border.Style>
                <Style TargetType="Border">
                    <Setter Property="Visibility" Value="Collapsed"/>
                    <Style.Triggers>
                        <MultiDataTrigger>
                            <MultiDataTrigger.Conditions>
                                <Condition Binding="{Binding ElementName=Search, Path=IsFocused}" Value="True"/>
                                <Condition Binding="{Binding ElementName=Search, Path=IsMouseOver}" Value="True"/>
                            </MultiDataTrigger.Conditions>
                            <Setter Property="Visibility" Value="Visible"/>
                        </MultiDataTrigger>
                        <MultiDataTrigger>
                            <MultiDataTrigger.Conditions>
                                <Condition Binding="{Binding ElementName=Search, Path=IsFocused}" Value="True"/>
                                <Condition Binding="{Binding RelativeSource={RelativeSource Self}, Path=IsMouseOver}" Value="True"/>
                            </MultiDataTrigger.Conditions>
                            <Setter Property="Visibility" Value="Visible"/>
                        </MultiDataTrigger>
                        <DataTrigger Binding="{Binding ElementName=QuestionMark, Path=IsMouseOver}" Value="True">
                            <Setter Property="Visibility" Value="Visible"/>
                        </DataTrigger>
                        <DataTrigger Binding="{Binding IsUsingAdvancedSearch}" Value="{x:Null}">
                            <Setter Property="Visibility" Value="Collapsed"/>
                        </DataTrigger>
                        <DataTrigger Binding="{Binding ElementName=SearchHint, Path=Text}" Value="">
                            <Setter Property="Visibility" Value="Collapsed"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </Border.Style>
            <Grid Margin="20">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <Rectangle Grid.Row="1" Grid.ColumnSpan="2" Margin="0,14,0,0" Height="1" Fill="#33FFFFFF" VerticalAlignment="Top" />
                <TextBlock x:Name="SearchHint" Text="{Binding StartTypingToSearchLabel}" TextWrapping="Wrap" FontStyle="Italic" FontFamily="LAUNCHBOX_THEME_FOLDER/Fonts/Poppins-Regular.ttf#Poppins" FontSize="14" Opacity="0.7" />
                <TextBlock Grid.Row="1" Margin="0,28,5,0" Text="{Binding Converter={StaticResource UpperCase}, Path=AdvancedSearchOptionsLabel}" FontFamily="LAUNCHBOX_THEME_FOLDER/Fonts/Poppins-Regular.ttf#Poppins" FontSize="14" Opacity="0.87" TextWrapping="Wrap"/>
                <TextBlock Grid.Row="2" Margin="0,15,0,0" Text="{Binding AdvancedSearchOptionsListLabel}" FontFamily="LAUNCHBOX_THEME_FOLDER/Fonts/Poppins-Regular.ttf#Poppins" FontSize="14" Opacity="0.87" TextWrapping="Wrap"/>
                <TextBlock Grid.Row="3" Margin="0,15,0,0" Text="{Binding AdvancedSearchOptionsExampleLabel}" FontFamily="LAUNCHBOX_THEME_FOLDER/Fonts/Poppins-Regular.ttf#Poppins" FontSize="14" Opacity="0.6" TextWrapping="Wrap"/>
                <Button x:Name="QuestionMark" Grid.Row="1" Grid.Column="1" Margin="0,28,0,0" HorizontalAlignment="Right" VerticalAlignment="Top" Cursor="Hand" Command="{Binding AdvancedSearchHelpCommand}" FocusVisualStyle="{StaticResource FocusVisualStyle}">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="{x:Type Button}">
                                        <Grid x:Name="Grid" Background="Transparent">
                                            <Viewbox Height="16" Width="16">
                                                <Path x:Name="Path" Fill="#90FFFFFF" Data="M12,22A10,10,0,1,0,2,12,10,10,0,0,0,12,22Zm0-2a1.5,1.5,0,1,1,1.5-1.5A1.5,1.5,0,0,1,12,20ZM8,8.994a3.907,3.907,0,0,1,2.319-3.645,4.061,4.061,0,0,1,3.889.316,4,4,0,0,1,.294,6.456,3.972,3.972,0,0,0-1.411,2.114,1,1,0,0,1-1.944-.47,5.908,5.908,0,0,1,2.1-3.2,2,2,0,0,0-.146-3.23,2.06,2.06,0,0,0-2.006-.14,1.937,1.937,0,0,0-1.1,1.8A1,1,0,0,1,8,9Z" />
                                            </Viewbox>
                                        </Grid>
                                        <ControlTemplate.Triggers>
                                            <Trigger SourceName="Grid" Property="IsMouseOver" Value="True">
                                                <Setter TargetName="Path" Property="Fill" Value="#AAFFFFFF" />
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </Button.Style>
                </Button>
            </Grid>
        </Border>
        <ComboBox Grid.Row="3" Name="ListTypeItems" Margin="20,0,20,0" ItemsSource="{Binding ListTypeItems}"
            SelectedItem="{Binding Path=SelectedListTypeItem, Mode=TwoWay}" FocusVisualStyle="{StaticResource FocusVisualStyle}" Panel.ZIndex="5">
            <ComboBox.ItemContainerStyle>
                <Style TargetType="ComboBoxItem">
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="ComboBoxItem">
                                <Grid Name="GridBd" Background="Transparent">
                                    <Border Name="Bd" BorderThickness="0" Background="{TemplateBinding Background}" HorizontalAlignment="Left"/>
                                    <Border Padding="12,8,12,8">
                                        <ContentPresenter  HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" />
                                    </Border>
                                </Grid>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsSelected" Value="True">
                                        <Setter TargetName="Bd" Property="Background" Value="{Binding ElementName=SelectionBorder, Path=Background}" />
                                        <Setter TargetName="Bd" Property="Width" Value="5" />
                                        <Setter TargetName="Bd" Property="CornerRadius" Value="2.5"/>
                                    </Trigger>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter TargetName="GridBd" Property="Background" Value="#11FFFFFF" />
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </ComboBox.ItemContainerStyle>
            <ComboBox.Style>
                <Style TargetType="ComboBox">
                    <Setter Property="SnapsToDevicePixels" Value="true"/>
                    <Setter Property="OverridesDefaultStyle" Value="true"/>
                    <Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Auto"/>
                    <Setter Property="ScrollViewer.VerticalScrollBarVisibility" Value="Auto"/>
                    <Setter Property="ScrollViewer.CanContentScroll" Value="true"/>
                    <Setter Property="MinWidth" Value="120"/>
                    <Setter Property="MinHeight" Value="20"/>
                    <Setter Property="Foreground" Value="{Binding ForegroundBrush}" />
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="ComboBox">
                                <Grid>
                                    <ToggleButton Name="ToggleButton" Focusable="false"
                                        IsChecked="{Binding Path=IsDropDownOpen,Mode=TwoWay,RelativeSource={RelativeSource TemplatedParent}}"
                                        ClickMode="Press">
                                        <ToggleButton.Template>
                                            <ControlTemplate TargetType="ToggleButton">
                                                <Grid>
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition />
                                                        <ColumnDefinition Width="30" />
                                                    </Grid.ColumnDefinitions>
                                                    <Border x:Name="Border" Grid.ColumnSpan="2" Background="#22FFFFFF" CornerRadius="4" />
                                                    <Path x:Name="Arrow" Grid.Column="1" Fill="{Binding FadedSearchForegroundBrush}"
                                                        HorizontalAlignment="Center" VerticalAlignment="Center" Data="M 0 0 L 4 4 L 8 0 Z" />
                                                </Grid>
                                                <ControlTemplate.Triggers>
                                                    <Trigger Property="ToggleButton.IsMouseOver" Value="true">
                                                        <Setter TargetName="Arrow" Property="Fill" Value="{Binding ForegroundBrush}" />
                                                        <Setter TargetName="Border" Property="Background" Value="#33FFFFFF" />
                                                    </Trigger>
                                                    <Trigger Property="ToggleButton.IsChecked" Value="true">
                                                        <Setter TargetName="Border" Property="Background" Value="#22FFFFFF" />
                                                    </Trigger>
                                                </ControlTemplate.Triggers>
                                            </ControlTemplate>
                                        </ToggleButton.Template>
                                    </ToggleButton>
                                    <ContentPresenter Name="ContentSite" IsHitTestVisible="False" Content="{TemplateBinding SelectionBoxItem}"
                                        ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                        ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}" Margin="12,8,12,8"
                                        VerticalAlignment="Center" HorizontalAlignment="Left" />
                                    <TextBox x:Name="PART_EditableTextBox" Style="{x:Null}" HorizontalAlignment="Left" VerticalAlignment="Center"
                                        Margin="12,8,12,8" Focusable="True" Background="Transparent" Visibility="Hidden"
                                        IsReadOnly="{TemplateBinding IsReadOnly}">
                                        <TextBox.Template>
                                            <ControlTemplate TargetType="TextBox">
                                                <Border x:Name="PART_ContentHost" Focusable="False" Background="{TemplateBinding Background}" />
                                            </ControlTemplate>
                                        </TextBox.Template>
                                    </TextBox>
                                    <Popup Name="Popup" Placement="Bottom" IsOpen="{TemplateBinding IsDropDownOpen}" AllowsTransparency="True"
                                        Focusable="False" PopupAnimation="Slide">
                                        <Grid Name="DropDown" SnapsToDevicePixels="True" MinWidth="{TemplateBinding ActualWidth}"
                                            MaxHeight="{TemplateBinding MaxDropDownHeight}">
                                            <Border x:Name="DropDownBorder" Background="#F50E1014" BorderBrush="#33363F" BorderThickness="1">
                                                <ScrollViewer Margin="0" SnapsToDevicePixels="True">
                                                    <StackPanel IsItemsHost="True" KeyboardNavigation.DirectionalNavigation="Contained" />
                                                    <ScrollViewer.Resources>
                                                        <Style TargetType="ScrollBar">
                                                            <Setter Property="Template">
                                                                <Setter.Value>
                                                                    <ControlTemplate TargetType="ScrollBar">
                                                                        <Track Name="PART_Track" IsDirectionReversed="true" Margin="5,5,5,5" ViewportSize="4">
                                                                            <Track.Resources>
                                                                                <system:Double x:Key="{x:Static SystemParameters.VerticalScrollBarButtonHeightKey}">120</system:Double>
                                                                            </Track.Resources>
                                                                            <Track.DecreaseRepeatButton>
                                                                                <RepeatButton Name="DecreaseRepeatButton" Command="ScrollBar.PageUpCommand" Background="{Binding ControlsBackgroundBrush}">
                                                                                    <RepeatButton.Template>
                                                                                        <ControlTemplate TargetType="RepeatButton">
                                                                                            <Grid>
                                                                                                <Border Name="Border" Background="#00000000" BorderThickness="0" />
                                                                                            </Grid>
                                                                                            <ControlTemplate.Triggers>
                                                                                                <Trigger SourceName="Border" Property="IsMouseOver" Value="True">
                                                                                                    <Setter TargetName="Border" Property="Background" Value="#22FFFFFF" />
                                                                                                </Trigger>
                                                                                            </ControlTemplate.Triggers>
                                                                                        </ControlTemplate>
                                                                                    </RepeatButton.Template>
                                                                                </RepeatButton>
                                                                            </Track.DecreaseRepeatButton>
                                                                            <Track.IncreaseRepeatButton>
                                                                                <RepeatButton Name="IncreaseRepeatButton" Command="ScrollBar.PageDownCommand" Background="{Binding ControlsBackgroundBrush}">
                                                                                    <RepeatButton.Template>
                                                                                        <ControlTemplate TargetType="RepeatButton">
                                                                                            <Grid>
                                                                                                <Border Name="Border" Background="#00000000" />
                                                                                            </Grid>
                                                                                            <ControlTemplate.Triggers>
                                                                                                <Trigger SourceName="Border" Property="IsMouseOver" Value="True">
                                                                                                    <Setter TargetName="Border" Property="Background" Value="#22FFFFFF" />
                                                                                                </Trigger>
                                                                                            </ControlTemplate.Triggers>
                                                                                        </ControlTemplate>
                                                                                    </RepeatButton.Template>
                                                                                </RepeatButton>
                                                                            </Track.IncreaseRepeatButton>
                                                                            <Track.Thumb>
                                                                                <Thumb>
                                                                                    <Thumb.Template>
                                                                                        <ControlTemplate TargetType="Thumb">
                                                                                            <Border Name="Border"  Background="{Binding FadedForegroundBrush}" BorderThickness="0" />
                                                                                            <ControlTemplate.Triggers>
                                                                                                <Trigger SourceName="Border" Property="IsMouseOver" Value="True">
                                                                                                    <Setter TargetName="Border" Property="Background" Value="#99FFFFFF" />
                                                                                                </Trigger>
                                                                                            </ControlTemplate.Triggers>
                                                                                        </ControlTemplate>
                                                                                    </Thumb.Template>
                                                                                </Thumb>
                                                                            </Track.Thumb>
                                                                        </Track>
                                                                        <ControlTemplate.Triggers>
                                                                            <Trigger SourceName="PART_Track" Property="IsEnabled" Value="false">
                                                                                <Setter TargetName="PART_Track" Property="Visibility" Value="Hidden"/>
                                                                            </Trigger>
                                                                        </ControlTemplate.Triggers>
                                                                    </ControlTemplate>
                                                                </Setter.Value>
                                                            </Setter>
                                                        </Style>
                                                    </ScrollViewer.Resources>
                                                </ScrollViewer>
                                            </Border>
                                        </Grid>
                                    </Popup>
                                </Grid>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsGrouping" Value="true">
                                        <Setter Property="ScrollViewer.CanContentScroll" Value="false"/>
                                    </Trigger>
                                    <Trigger SourceName="Popup" Property="Popup.AllowsTransparency" Value="true">
                                        <Setter TargetName="DropDownBorder" Property="Margin" Value="0,2,0,0"/>
                                    </Trigger>
                                    <Trigger Property="IsEditable" Value="true">
                                        <Setter Property="IsTabStop" Value="false"/>
                                        <Setter TargetName="PART_EditableTextBox" Property="Visibility" Value="Visible" />
                                        <Setter TargetName="ContentSite" Property="Visibility" Value="Hidden" />
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </ComboBox.Style>
        </ComboBox>
        <TreeView Grid.Row="4" Grid.RowSpan="2" Name="Items" Margin="20,20,20,20" Background="Transparent"
            ItemsSource="{Binding Nodes}" cal:Message.Attach="[Event SelectedItemChanged] = [Action OnSelectedItemChanged($eventArgs)]"
            HorizontalContentAlignment="Stretch" HorizontalAlignment="Stretch" VirtualizingStackPanel.IsVirtualizing="True" BorderThickness="0" Panel.ZIndex="10">
            <TreeView.ItemTemplate>
                <HierarchicalDataTemplate ItemsSource="{Binding Nodes}">
                    <DockPanel LastChildFill="True" HorizontalAlignment="Stretch">
                        <TextBlock Padding="12,8,12,8" Text="{Binding Count}" TextAlignment="Right" DockPanel.Dock="Right" Visibility="{Binding RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type UserControl}}, Path=DataContext.RightCountVisibility}" />
                        <Grid DockPanel.Dock="Left" Margin="12,0,0,0"  Height="24" Width="24" Visibility="{Binding Source={StaticResource IconVisibility}, Path=Data}">
                            <controls:PlatformIcon PlatformName="{Binding BackendValue}" PlatformType="{Binding FilterType}" />
                        </Grid>
                        <TextBlock Padding="12,8,12,8" Text="{Binding Value}" TextAlignment="Left" TextTrimming="CharacterEllipsis" Visibility="{Binding RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type UserControl}}, Path=DataContext.ValueWithoutCountVisibility}" />
                        <TextBlock Padding="12,8,12,8" Text="{Binding ValueWithCount}" TextAlignment="Left" TextTrimming="CharacterEllipsis" Visibility="{Binding RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type UserControl}}, Path=DataContext.ValueWithCountVisibility}" />
                    </DockPanel>
                </HierarchicalDataTemplate>
            </TreeView.ItemTemplate>
            <TreeView.Style>
                <Style TargetType="TreeView">
                    <Setter Property="BorderThickness" Value="0" />
                    <Setter Property="Foreground" Value="{Binding ForegroundBrush}" />
                    <Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Disabled" />
                    <Setter Property="ScrollViewer.VerticalScrollBarVisibility" Value="{Binding ScrollBarVisibility}" />
                </Style>
            </TreeView.Style>
            <TreeView.Resources>
                <Style TargetType="ScrollBar">
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="ScrollBar">
                                <Track Name="PART_Track" IsDirectionReversed="true" Margin="10,1,-7,2" ViewportSize="4">
                                    <Track.Resources>
                                        <system:Double x:Key="{x:Static SystemParameters.VerticalScrollBarButtonHeightKey}">120</system:Double>
                                    </Track.Resources>
                                    <Track.DecreaseRepeatButton>
                                        <RepeatButton Name="DecreaseRepeatButton" Command="ScrollBar.PageUpCommand"
                                                      Background="{Binding ControlsBackgroundBrush}">
                                            <RepeatButton.Template>
                                                <ControlTemplate TargetType="RepeatButton">
                                                    <Grid>
                                                        <Border Name="Border" Background="#00000000" BorderThickness="0" />
                                                    </Grid>
                                                    <ControlTemplate.Triggers>
                                                        <Trigger SourceName="Border" Property="IsMouseOver" Value="True">
                                                            <Setter TargetName="Border" Property="Background" Value="#22FFFFFF" />
                                                        </Trigger>
                                                    </ControlTemplate.Triggers>
                                                </ControlTemplate>
                                            </RepeatButton.Template>
                                        </RepeatButton>
                                    </Track.DecreaseRepeatButton>
                                    <Track.IncreaseRepeatButton>
                                        <RepeatButton Name="IncreaseRepeatButton" Command="ScrollBar.PageDownCommand"
                                            Background="{Binding ControlsBackgroundBrush}">
                                            <RepeatButton.Template>
                                                <ControlTemplate TargetType="RepeatButton">
                                                    <Grid>
                                                        <Border Name="Border" Background="#00000000" />
                                                    </Grid>
                                                    <ControlTemplate.Triggers>
                                                        <Trigger SourceName="Border" Property="IsMouseOver" Value="True">
                                                            <Setter TargetName="Border" Property="Background" Value="#22FFFFFF" />
                                                        </Trigger>
                                                    </ControlTemplate.Triggers>
                                                </ControlTemplate>
                                            </RepeatButton.Template>
                                        </RepeatButton>
                                    </Track.IncreaseRepeatButton>
                                    <Track.Thumb>
                                        <Thumb>
                                            <Thumb.Template>
                                                <ControlTemplate TargetType="Thumb">
                                                    <Border Name="Border"  Background="{Binding FadedForegroundBrush}" BorderThickness="0" CornerRadius="4" />
                                                    <ControlTemplate.Triggers>
                                                        <Trigger SourceName="Border" Property="IsMouseOver" Value="True">
                                                            <Setter TargetName="Border" Property="Background" Value="#99FFFFFF" />
                                                        </Trigger>
                                                    </ControlTemplate.Triggers>
                                                </ControlTemplate>
                                            </Thumb.Template>
                                        </Thumb>
                                    </Track.Thumb>
                                </Track>
                                <ControlTemplate.Triggers>
                                    <Trigger SourceName="PART_Track" Property="IsEnabled" Value="false">
                                        <Setter TargetName="PART_Track" Property="Visibility" Value="Hidden"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
                <system:Double x:Key="{x:Static SystemParameters.VerticalScrollBarWidthKey}">10</system:Double>
            </TreeView.Resources>
        </TreeView>
    </Grid>
</UserControl>