<UserControl xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="300"
    Focusable="False">
    <!--
        HLTB Theme: Solid background.
        The blurred game artwork background has been intentionally suppressed
        so the UI presents a clean, single-tone surface. The image element is
        retained (Collapsed) for binding compatibility with LaunchBox.
    -->
    <Grid Background="#1B1D24">
        <Image Name="Image"
               Visibility="Collapsed"
               HorizontalAlignment="{Binding HorizontalAlignment}"
               VerticalAlignment="{Binding VerticalAlignment}"
               Stretch="{Binding Stretch}"
               RenderOptions.BitmapScalingMode="HighQuality" />
    </Grid>
</UserControl>
